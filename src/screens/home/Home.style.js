import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    borderColor: 'pink',
    borderWidth: 1,
    flex: 1,
    alignItems: 'center',
    flexDirection: 'row',
    width: '100%',
  },
});
